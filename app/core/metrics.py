"""Simple metrics utilities."""

from typing import Any


def update_database_metrics(health_data: dict[str, Any]) -> None:
    """Update database metrics (no-op for now)."""


def update_health_metrics(checks: dict[str, Any]) -> None:
    """Update health metrics (no-op for now)."""
