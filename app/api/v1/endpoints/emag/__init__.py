"""eMAG API endpoints."""

from fastapi import APIRouter

# from app.api.v1.endpoints.emag import imports as import_endpoints
# from app.api.v1.endpoints.emag import integration

router = APIRouter()
# router.include_router(mappings.router, prefix="/emag", tags=["emag"])
# router.include_router(import_endpoints.router, prefix="/emag", tags=["emag-import"])
# router.include_router(integration.router, prefix="/emag", tags=["emag-integration"])
